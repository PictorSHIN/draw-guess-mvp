# 《你演我猜》产品需求文档（PRD）

- **文档版本**：v2.0
- **日期**：2026-08-31
- **产品形态**：微信小程序（单机离线可玩，零后端）
- **一句话定位**：线下朋友局常备的 Heads Up 式派对游戏——手机举额头面向队友，队友描述、举机者猜，靠翻转手机计分。
- **变更说明**：v2.0 将产品目标从 H5 网页调整为**微信小程序上架**；核心玩法不变，技术栈与分发、审核要求全面更新。

---

## 1. 背景与目标

### 1.1 背景
线下朋友聚会缺少低门槛、即开即玩、能带动全场互动的小游戏。H5 原型已验证核心玩法（体感翻牌、11 主题题库、暗黑霓虹 UI），但 H5 依赖 HTTPS 自签证书、浏览器授权弹窗、PWA 添加入口，分发与留存门槛较高。

**转向微信小程序的原因：**
- 微信内一键打开，无需安装、无需配置证书。
- 原生传感器 API 与震动反馈，体验更稳定。
- 可通过搜索、分享、最近使用触达，利于传播。
- 符合「聚会小游戏」的产品定位。

### 1.2 产品目标
- **核心目标**：激发线下聚会的互动乐趣，营造轻竞技的比拼氛围。
- **上架目标**：通过微信小程序审核，正式上架可供搜索与分享。
- **非目标（MVP）**：不以商业变现为目标；不做账号体系、社交关系链、排行榜、付费、多设备联机。

### 1.3 成功衡量
| 维度 | 标准 |
|---|---|
| 开玩效率 | 从打开小程序到进入游戏 ≤ 30 秒 |
| 体感手感 | 翻牌判定准确，误触发少，无连触多题 |
| 内容耐玩 | 11 主题 × 200 词，连续多局不感重复 |
| 审核通过 | 首次提审通过或按要求一次改完即过 |

---

## 2. 目标用户与场景

### 2.1 目标用户
- 线下聚会的年轻朋友群体（3 人及以上）。
- 微信用户，无需注册，打开即玩。

### 2.2 核心场景
1. 用户在微信中搜索小程序名，或扫描/点击分享卡片进入。
2. 一台手机在人群中传着玩。
3. 当前「举机者」把手机横举到额头，屏幕面向其他队友。
4. 屏幕显示题目词，举机者看不到；队友用语言/动作描述。
5. 举机者猜：
   - 猜对 → 手机**向下翻** → 计 1 分，自动下一题。
   - 想放弃 → 手机**向上翻** → 跳过，自动下一题。
6. 倒计时结束，结算本轮得分与逐题回顾。
7. 手机传给下一个人，开新一局；或分享小程序邀请朋友。

### 2.3 入口与分发
| 入口 | 说明 |
|---|---|
| 微信搜索 | 小程序名称搜索（需审核通过后生效） |
| 分享卡片 | 结算页/首页「分享给朋友」（P1，MVP 可预留接口） |
| 最近使用 | 用户再次打开微信 → 发现 → 小程序 |

---

## 3. 产品范围

### 3.1 MVP 范围（微信小程序 v1.0）

**从 H5 迁移保留：**
- 11 个主题 + 3 档难度（运行时按词序三等分）+ 倒计时档位与滑块联动。
- 3 秒开局倒数。
- 体感翻牌（下翻答对 / 上翻跳过）+ 屏幕按钮兜底。
- 倒计时进度条 + 实时比分 + 翻牌视觉/震动/音效反馈。
- 最后 10 秒倒计时冲刺（红色脉动 + 滴答提示）。
- 答题页「退出」→ 走结算页。
- 结算页：得分 + 逐题回顾 + 再来一局 / 回首页。
- 内置静态题库（2200 词，洗牌不重复），provider 层抽象。

**微信小程序新增/调整：**
- 原生页面结构（见 §6.2），替代 H5 单文件 SPA。
- 使用 `wx.startAccelerometer` + `wx.onAccelerometerChange` 实现翻牌判定（对齐 H5 重力 z 轴方案）。
- 使用 `wx.vibrateShort` 替代 `navigator.vibrate`。
- 游戏页强制横屏（`pageOrientation: landscape`）。
- 竖屏进入游戏页时提示「请把手机横过来」。
- 隐私协议弹窗（微信审核必需）。
- 小程序基本信息：名称、简介、图标、类目、服务条款与隐私政策页面。

**明确不做（本期）：**
- 多设备联机对局、分队对抗、账号体系、云端数据、排行榜。
- 微信开放数据域 / 好友排行。
- 大模型实时生成题目（架构预留 provider 接口，本期静态题库）。
- 激励视频广告、内购等变现能力。

### 3.2 后续规划（Roadmap）

| 阶段 | 内容 |
|---|---|
| P1 | 分享卡片（带主题/得分摘要）、自定义题库、AI 实时生成题目 |
| P2 | 历史最高分（本地 Storage）、分队对抗模式、更多音效主题 |
| P3 | 多设备联机（需后端 + WebSocket）、好友对战 |

---

## 4. 功能需求详述

### 4.1 首页 / 开局设置

| 项 | 需求 |
|---|---|
| 主题卡片 | 展示 **11** 类主题（明星名人、影视作品、动物世界、美食小吃、动作表演、职业身份、成语俗语、网络热梗、品牌商标、地点景点、二次元）。每卡显示名称、题量（200）。单选，可滚动。 |
| 难度 | 简单 / 中等 / 困难，三选一，默认「简单」。 |
| 倒计时档位 | 60s / 90s / 120s，快捷选择。 |
| 倒计时滑块 | 30–180s，步进 5s，可微调；与档位联动。默认 90s。 |
| 开始按钮 | 未选主题时禁用（置灰）。选中主题后可点击。 |
| 隐私入口 | 页脚或设置区提供「隐私政策」「用户协议」链接。 |

**验收标准**
- 未选主题时无法开始。
- 选择档位后滑块同步；拖动滑块后档位高亮随之更新。
- 首次进入或未同意隐私协议时，弹出隐私授权弹窗。

### 4.2 准备页

| 项 | 需求 |
|---|---|
| 玩法说明 | 精简提示：横举额头、屏幕面向队友；下翻=答对、上翻=跳过。 |
| 传感器启动 | 点击「准备好了」时，在用户手势回调内调用 `wx.startAccelerometer({ interval: 'game' })`；失败则降级为按钮模式。 |
| 横屏提示 | 若当前为竖屏，显示「请把手机横过来」遮罩；监听 `wx.onWindowResize` 或页面 orientation 变化后隐藏。 |
| 返回 | 可返回首页重新设置。 |

**验收标准**
- iOS / Android 真机点击「准备好了」后传感器正常回调。
- 传感器不可用或用户拒绝授权时，自动显示「答对 / 跳过」按钮兜底，不阻塞游戏。
- 进入游戏页后保持横屏展示。

### 4.3 开局倒数
- 进入游戏前 3→2→1 大字倒数，每步约 0.9s。
- 倒数期间不接受翻牌/答题。

### 4.4 游戏页（核心）

| 区域 | 需求 |
|---|---|
| 顶部倒计时条 | 进度条随剩余时间收缩；文字显示剩余秒数。 |
| 实时比分 | 左上角显示答对数、跳过数。 |
| 词卡 | 居中大字自适应（`vmin` / rpx），保证远处可读；下方显示「第 N / 总数 题」。 |
| 翻牌反馈 | 答对绿色闪光「答对！」，跳过粉色闪光「跳过」，约 1s；伴随震动 + 音效。 |
| 体感判定 | 读取加速度计 **z 轴**（屏幕法线方向重力分量）：`z > 阈值` 判答对，`z < -阈值` 判跳过（对齐 H5 已验证方案，默认阈值 ±5.5，可调）。 |
| 防连触 | 三道锁：① 触发后须回中（\|z\| < 回中阈值）且锁定期结束才重新武装；② 切题锁定 ~950ms；③ 两次触发最小间隔 ~1100ms。 |
| 兜底按钮 | 传感器不可用时，屏幕底部显示「答对↓ / 跳过↑」两个大按钮。 |
| 退出 | 右上角「✕ 退出」，走结算页展示已答对/跳过统计。 |
| 结束条件 | 倒计时归零、题目用尽、或点击退出，进入结算。 |
| 冲刺 | 最后 10 秒：倒计时变红脉动 + 进度条闪烁 + 每秒滴答音效/短震。 |

**验收标准**
- 倒计时归零 / 题目用尽 / 退出均正确进入结算。
- 体感与按钮两种方式均可计分、切题、反馈。
- 快速连续翻转不会一次误加多分。

### 4.5 结算页

| 项 | 需求 |
|---|---|
| 本轮得分 | 大字显示答对数。 |
| 概览 | 「答对 X · 跳过 Y」。 |
| 逐题回顾 | 列出本轮每题：答对✅（绿色）/ 跳过⏭（灰色删除线）。无答题时显示占位文案。 |
| 操作 | 「再来一局」（回准备页，沿用当前设置）、「回首页」（重新设置）。 |
| 分享（P1 预留） | MVP 可隐藏；架构预留 `onShareAppMessage` 钩子。 |

**验收标准**
- 得分、答对/跳过数、逐题列表与实际过程一致。

---

## 5. 题库设计

### 5.1 数据结构
```js
// data/words.js → 构建为 TOPICS
TOPICS = {
  <topicId>: {
    name,
    words: [ ...200 词，按易→难排列 ]
  }
}
// 运行时三等分为 easy / mid / hard
```

### 5.2 内容要求
- **11 主题 × 200 词 = 2200 词**，零重复。
- 词条以「便于表演/描述」为原则；难度递增表现为知名度降低、抽象度提高。
- **微信审核合规**：避免政治敏感、色情暴力、赌博、侵权明星/品牌（商标类主题需注意描述性用词）、违禁词汇。提审前需人工抽检题库。

### 5.3 洗牌与去重
- 每局 Fisher-Yates 洗牌，同一局内不重复出题。
- 题目用尽则提前结束本局。

### 5.4 抽象层（关键设计）
- 题目获取统一走 `getWords(topicId, diffId)` provider。
- 未来接入 AI 或云题库时，替换为 `async getWords()` 调用云函数，上层游戏逻辑不变。

### 5.5 包体积策略
- 2200 词 JSON/JS 预估 ~200–400KB，主包可承载。
- 若后续扩题或加资源，采用**分包加载**（按主题分包），单包 ≤ 2MB，总包 ≤ 20MB。

---

## 6. 技术方案

### 6.1 技术栈

| 项 | 方案 |
|---|---|
| 框架 | 微信原生小程序（WXML + WXSS + JS），或 uni-app / Taro 二选一（**推荐原生**：包体小、传感器 API 直调、审核风险低） |
| 后端 | 无。MVP 纯前端，题库内置。 |
| 云能力 | 不使用云开发（MVP）。P1 AI 生成时可接入云函数。 |
| 存储 | 可选 `wx.setStorageSync` 缓存用户上次主题/难度/倒计时偏好。 |

### 6.2 页面结构

```
pages/
├── index/          # 首页（设置：主题、难度、倒计时）
├── prepare/        # 准备页（玩法说明、启动传感器）
├── countdown/      # 3 秒倒数（可与 game 合并为同一页内状态机）
├── game/           # 游戏页（横屏，核心答题）
├── result/         # 结算页
└── legal/
    ├── privacy/    # 隐私政策
    └── terms/      # 用户协议
```

> 实现时 `countdown` 可与 `game` 合并为单页多状态，减少页面跳转延迟；以「开玩 ≤ 30 秒」为目标。

### 6.3 传感器方案

| 项 | H5（已验证） | 微信小程序 |
|---|---|---|
| API | `devicemotion` → `accelerationIncludingGravity.z` | `wx.startAccelerometer` + `wx.onAccelerometerChange` → `res.z` |
| 判定 | z > 5.5 答对，z < -5.5 跳过 | 同左，阈值可配置 |
| 防连触 | 回中武装 + 950ms 锁 + 1100ms 冷却 | 同左，逻辑迁移 |
| 兜底 | `body.no-gyro` 显示按钮 | 传感器 start 失败或无回调时显示按钮 |
| 生命周期 | — | 游戏页 `onShow` 启动、`onHide`/`onUnload` 停止传感器，防后台耗电 |

### 6.4 音效与震动

| 能力 | 方案 |
|---|---|
| 震动 | `wx.vibrateShort({ type: 'heavy' \| 'medium' })` |
| 音效 | 小程序 InnerAudioContext 播放短音效，或 WebAudio 合成（需真机验证 iOS 兼容）；MVP 优先 InnerAudioContext + 内置短音频（注意包体积） |
| 静音 | 尊重系统静音；设置页可提供「音效开关」（P1）。 |

### 6.5 横屏与适配

| 项 | 方案 |
|---|---|
| 游戏页横屏 | `game.json` 设置 `"pageOrientation": "landscape"` |
| 首页/结算 | 默认竖屏；横屏时左右分栏（沿用 H5 布局逻辑） |
| 安全区 | 使用 `env(safe-area-inset-*)` 适配刘海屏 |
| 基础库 | 最低基础库版本 **2.19.0+**（加速度计 game  interval、横屏配置） |

### 6.6 从 H5 迁移对照

| H5 能力 | 小程序替代 |
|---|---|
| PWA 全屏 | 小程序原生全屏，无需 PWA |
| `server.js` HTTPS | 删除，开发者工具 + 真机预览即可 |
| `manifest.webmanifest` | `app.json` + 小程序后台配置 |
| `navigator.vibrate` | `wx.vibrateShort` |
| `deviceorientation` / `devicemotion` | `wx.onAccelerometerChange` |
| 单文件 `index.html` | 多页面 + 公共 `utils/`、`data/` |

---

## 7. 微信小程序上架要求

### 7.1 基本信息

| 项 | 要求 |
|---|---|
| 小程序名称 | 「你演我猜」或过审备选名（见 §11 开放项） |
| 简介 | 一句话说明聚会猜词玩法，≤ 120 字 |
| 图标 | 1024×1024，符合微信设计规范，暗黑霓虹风格 |
| 服务类目 | **工具 > 效率**（已确认；规避游戏类目版号要求，按「线下聚会辅助工具」定位提审） |
| 标签 | 聚会、猜词、派对、效率工具 |
| 提审表述 | 简介与审核说明强调「聚会破冰 / 互动辅助」，避免使用「游戏」「对战」「竞技」等易触发游戏类目审核的措辞 |

### 7.2 审核合规清单

| 项 | 说明 |
|---|---|
| 隐私政策 | 必须提供独立页面；说明收集的信息（MVP 仅本地传感器，不上传个人数据） |
| 用户协议 | 建议提供，明确内容仅供娱乐 |
| 用户隐私保护指引 | 在微信公众平台配置，声明使用的权限（如：设备传感器） |
| 内容安全 | 题库人工抽检，剔除敏感词；无 UGC 则无需内容安全 API |
| 测试账号 | 纯单机无需提供；若后续有登录则需提供 |
| 功能完整性 | 提审版本功能完整可用，无「开发中」占位 |

### 7.3 权限声明

| 权限 | 用途 | 是否必需 |
|---|---|---|
| 设备传感器（加速度计） | 体感翻牌判定 | 是（可降级按钮） |
| 震动 | 翻牌反馈 | 否（增强体验） |

### 7.4 提审流程

1. 注册微信小程序账号（个人或企业均可；**工具 > 效率** 对个人主体通常可用）。
2. 公众平台选择服务类目：**工具 > 效率**，按指引提交类目资质（一般无需版号）。
3. 开发者工具上传代码 → 提交审核。
4. 填写版本说明、测试路径（首页 → 选主题 → 开始 → 完成一局）；版本说明示例：「线下聚会猜词辅助工具，单机使用，无联网对战」。
5. 审核通过后发布；不通过则按反馈修改后重新提审。

---

## 8. 视觉与交互规范

- **风格**：暗黑霓虹风。深色背景 + 霓虹粉/青/紫/绿/黄点缀，发光文字与按钮（沿用 H5 视觉）。
- **可读性**：词卡大字、按钮大而易点，保证「举在额头、队友远处也看得清」。
- **反馈**：翻牌即时的颜色闪光 + 震动 + 音效，强化派对氛围。
- **动效**：页面切换淡入、倒数大字发光、答对/跳过弹跳闪光（使用 CSS animation / wx.createAnimation）。
- **微信规范**：按钮热区 ≥ 44px；不使用误导性分享文案；符合微信小程序设计指南。

---

## 9. 状态流转

```
首页(设置) → 准备页(启动传感器+说明) → 3秒倒数 → 游戏页(答题) → 结算页
                ↑ 返回                                    ↓ 再来一局
                └────────────────────────────────────────┘（沿用设置回准备页）
结算页 → 回首页（重新设置）
游戏页 →(✕ 退出)→ 结算页
```

---

## 10. 埋点与度量

| 阶段 | 方案 |
|---|---|
| MVP | 不做自定义埋点；可依赖微信小程序后台「访问分析」。 |
| P1+ | 接入 `wx.reportAnalytics` 或 We 分析：每局时长、答对率、主题/难度分布、单局完成率。 |

---

## 11. 验收清单（微信小程序 v1.0）

### 功能
- [ ] 首页 11 主题、三难度、档位+滑块设置齐全且联动正确。
- [ ] 未选主题无法开始；隐私协议弹窗与跳转页正常。
- [ ] 准备页在 iOS/Android 真机能启动加速度计；失败时按钮兜底可用。
- [ ] 游戏页横屏展示；竖屏有旋转提示。
- [ ] 3 秒倒数正常。
- [ ] 体感下翻/上翻判定正确，无连触多题。
- [ ] 倒计时归零 / 题目用尽 / 退出均正确进入结算。
- [ ] 结算得分、答对/跳过数、逐题回顾与实际一致。
- [ ] 再来一局 / 回首页流转正确。
- [ ] 题库洗牌，单局不重复。

### 上架
- [ ] 隐私政策页、用户协议页已上线且可从首页访问。
- [ ] 小程序后台权限声明与隐私指引已配置。
- [ ] 图标、名称、简介已填写；服务类目已选 **工具 > 效率**。
- [ ] 开发者工具上传成功，真机预览无崩溃。
- [ ] 提审通过并正式发布。

---

## 12. 待确认 / 开放项

1. **小程序名称**：「你演我猜」是否可用；若被占用需备选名（如「聚会猜词助手」）。
2. **开发框架**：原生 vs uni-app/Taro（影响迁移成本与包体积）。
3. **音效方案**：WebAudio 合成 vs 内置短音频文件（iOS 兼容性 vs 包体积）。
4. **体感阈值**：从 H5 迁移后需微信真机重新联调 z 轴阈值与回中判定。
5. **分享能力**：MVP 是否上线「分享给朋友」入口。
6. **主体类型**：个人小程序 vs 企业小程序（工具类目两者均可，企业主体后续扩展能力更全）。
