# 《你演我猜》产品需求文档（PRD）

- **文档版本**：v1.0
- **日期**：2026-08-31
- **产品形态**：纯前端 H5 移动端网页应用（单页应用，打开即玩、离线可用、零后端）
- **一句话定位**：线下朋友局常备的 Heads Up 式派对游戏——手机举额头面向队友，队友描述、举机者猜，靠翻转手机计分。

---

## 1. 背景与目标

### 1.1 背景
线下朋友聚会缺少低门槛、即开即玩、能带动全场互动的小游戏。市面上的猜词游戏多为固定题库、需下载安装、或强联网，不适合"掏出手机马上开一局"的场景。

### 1.2 产品目标
- **核心目标**：激发线下聚会的互动乐趣，营造轻竞技的比拼氛围。
- **非目标**：不以商业变现为目标；MVP 阶段不做账号、社交、排行榜、付费。

### 1.3 成功衡量（定性）
- 一局游戏能在 30 秒内从"打开"进入"开玩"。
- 陀螺仪翻牌手感自然、误触发少。
- 一群人能连续玩多局不感到题目重复枯燥。

---

## 2. 目标用户与场景

### 2.1 目标用户
- 线下聚会的年轻朋友群体（3 人及以上）。
- 无需注册、无学习成本，扫码/开链接即玩。

### 2.2 核心场景
1. 一台手机在人群中传着玩。
2. 当前"举机者"把手机横举到额头，屏幕面向其他队友。
3. 屏幕显示题目词，举机者看不到（对着队友）；队友用语言/动作描述。
4. 举机者猜：
   - 猜对 → 手机**向下翻** → 计 1 分，自动下一题。
   - 想放弃 → 手机**向上翻** → 跳过，自动下一题。
5. 倒计时结束，结算本轮得分与逐题回顾。
6. 手机传给下一个人，开新一局。

---

## 3. 产品范围

### 3.1 MVP 范围（本期）
- 主题选择（4 类）+ 难度选择（3 档）+ 倒计时设置（档位 + 滑块）。
- 3 秒开局倒数。
- 陀螺仪翻牌（下翻答对/上翻跳过）+ 屏幕按钮兜底。
- 倒计时进度条 + 实时比分 + 翻牌视觉/震动反馈。
- 结算页：得分 + 逐题回顾 + 再来一局/回首页。
- 内置静态题库（洗牌不重复），题库来源抽象为 provider 层。

### 3.2 明确不做（本期外）
- 多设备联机对局。
- 分队对抗计分（仅单机轮流、单人计分）。
- 账号体系、云端数据、排行榜、分享战绩。
- 大模型实时生成题目（**架构预留接口，本期用静态题库**）。

### 3.3 后续规划（Roadmap）
- P1：AI 实时生成题目（替换 provider 实现）、自定义题库导入。
- P2：分队对抗模式、历史战绩、音效。
- P3：多设备联机。

---

## 4. 功能需求详述

### 4.1 首页 / 开局设置

| 项 | 需求 |
|---|---|
| 主题卡片 | 展示 4 类主题：①人物/明星 ②动作/职业 ③成语/影视/万物 ④网络热梗/玩笑。每卡显示 emoji、名称、题量。单选。 |
| 难度 | 简单 / 中等 / 困难，三选一，默认"简单"。 |
| 倒计时档位 | 60s / 90s / 120s，快捷选择。 |
| 倒计时滑块 | 30–180s，步进 5s，可微调；与档位联动。默认 90s。 |
| 开始按钮 | 未选主题时禁用（置灰）。选中主题后可点击。 |

**验收标准**
- 未选主题时无法开始。
- 选择档位后滑块同步显示对应值；拖动滑块后档位高亮随之更新。

### 4.2 准备页

| 项 | 需求 |
|---|---|
| 玩法说明 | 图文提示：横举额头、屏幕面向队友；下翻=答对、上翻=跳过。 |
| 传感器授权 | iOS 13+ 等需授权的设备，显示授权提示；点"准备好了"时在用户手势内申请 `DeviceOrientationEvent.requestPermission`。 |
| 返回 | 可返回首页重新设置。 |

**验收标准**
- iOS 真机点击"准备好了"能弹出系统授权弹窗。
- 桌面/不支持陀螺仪的设备不阻塞，可继续用按钮玩。

### 4.3 开局倒数
- 进入游戏前 3→2→1 大字倒数，每步约 0.9s。
- 倒数期间不接受翻牌/答题。

### 4.4 游戏页（核心）

| 区域 | 需求 |
|---|---|
| 顶部倒计时条 | 进度条随剩余时间收缩；文字显示剩余秒数。 |
| 实时比分 | 显示当前答对数、跳过数。 |
| 词卡 | 居中大字显示当前题目词；下方显示"第 N / 总数 题"。 |
| 翻牌反馈 | 答对绿色闪光"答对！"，跳过粉色闪光"跳过"，约 0.5s；伴随震动（答对/跳过震动模式不同）。 |
| 陀螺仪判定 | 前后倾角 beta：>55° 判答对，<-55° 判跳过。触发后进入冷却，回正后方可再次触发（防连触）。 |
| 兜底按钮 | 屏幕底部"答对↓/跳过↑"两个大按钮，功能等价于陀螺仪，供桌面或无授权时使用。 |
| 结束条件 | 倒计时归零，或题目用尽，进入结算。 |

**交互细节**
- 翻牌防抖：单次判定后锁定约 480ms，避免一次翻转触发多题。
- 陀螺仪冷却：触发后约 900ms 内不再响应，防止一次翻转连续计分。
- 每题记录 `{ 词, 是否答对 }` 供结算回顾。

**验收标准**
- 倒计时归零自动进入结算。
- 陀螺仪与按钮两种方式都能正常计分、切题、反馈。
- 快速连续翻转不会一次误加多分。

### 4.5 结算页

| 项 | 需求 |
|---|---|
| 本轮得分 | 大字显示答对数。 |
| 概览 | "答对 X · 跳过 Y"。 |
| 逐题回顾 | 列出本轮每题：答对✅（绿色）/跳过⏭（灰色删除线）。无答题时显示占位文案。 |
| 操作 | "再来一局"（回准备页，沿用当前设置）、"回首页"（重新设置）。 |

**验收标准**
- 得分、答对/跳过数、逐题列表与实际过程一致。

---

## 5. 题库设计

### 5.1 数据结构
```
TOPICS = {
  <topicId>: {
    name, emoji,
    words: {
      easy: [ ... ],
      mid:  [ ... ],
      hard: [ ... ]
    }
  }
}
```

### 5.2 内容要求
- 4 主题 × 3 难度。MVP 每档目标 30–50 题（原型示意为每档 10 题）。
- 词条以"便于表演/描述"为原则；难度递增表现为知名度降低、抽象度提高。
- 内容适宜线下朋友场景，避免敏感、冒犯内容。

### 5.3 洗牌与去重
- 每局用 Fisher-Yates 洗牌，同一局内不重复出题。
- 题目用尽则提前结束本局（或后续可支持循环，MVP 先提前结束）。

### 5.4 抽象层（关键设计）
- 题目获取统一走 `getWords(topicId, diffId)` provider。
- 未来接入 AI 实时生成时，仅替换该函数为异步实现（如 `async getWords()` 调用生成接口），上层游戏逻辑不变。

---

## 6. 技术方案

| 项 | 方案 |
|---|---|
| 形态 | 纯前端 H5 单页应用，单文件即可运行；无后端、无 Key、离线可用。 |
| 陀螺仪 | `deviceorientation` 事件读取 beta（前后倾角）；iOS 13+ 需 `requestPermission`（用户手势内触发）。 |
| 震动 | `navigator.vibrate`（支持的设备）。 |
| 分发 | 静态托管（本地 http server / 任意静态托管），扫码或链接打开。 |
| 兼容 | 桌面无陀螺仪时用按钮兜底，保证全平台可玩。 |

**风险与对策**
- 陀螺仪阈值/回正手感因人而异 → 提供按钮兜底，阈值与冷却参数可调，真机联调后微调。
- iOS 授权仅能在用户手势内申请 → 放在"准备好了"按钮点击时申请。
- HTTPS 限制：部分浏览器传感器需安全上下文 → 生产环境用 HTTPS 托管。

---

## 7. 视觉与交互规范

- **风格**：暗黑霓虹风。深色背景 + 霓虹粉/青/紫/绿/黄点缀，发光文字与按钮。
- **可读性**：词卡大字、按钮大而易点，保证"举在额头、队友远处也看得清"。
- **反馈**：翻牌即时的颜色闪光 + 震动，强化派对氛围。
- **动效**：页面切换淡入、倒数大字发光、答对/跳过弹跳闪光。

---

## 8. 状态流转

```
首页(设置) → 准备页(授权+说明) → 3秒倒数 → 游戏页(答题) → 结算页
                ↑ 返回                              ↓ 再来一局
                └──────────────────────────────────┘（沿用设置回准备页）
结算页 → 回首页（重新设置）
```

---

## 9. 埋点与度量（后续可选）
MVP 不做埋点。后续如需评估，可记录：每局时长、答对率、主题/难度分布、单局是否玩完。

---

## 10. 验收清单（MVP 交付标准）
- [ ] 首页四主题、三难度、档位+滑块设置齐全且联动正确。
- [ ] 未选主题无法开始。
- [ ] 准备页在 iOS 真机能触发传感器授权。
- [ ] 3 秒倒数正常。
- [ ] 陀螺仪下翻/上翻能正确判定，且无一次翻转多计分问题。
- [ ] 按钮兜底在桌面/无授权设备可用。
- [ ] 倒计时归零/题目用尽正确进入结算。
- [ ] 结算得分、答对/跳过数、逐题回顾与实际一致。
- [ ] 再来一局/回首页流转正确。
- [ ] 题库洗牌，单局不重复。

---

## 11. 待确认 / 开放项
1. 陀螺仪触发阈值（当前 ±55°）与回正判定逻辑，需真机联调后定参。
2. 题库正式体量（每档 30 还是 50 题）。
3. 是否需要音效（本期默认仅震动+视觉）。
